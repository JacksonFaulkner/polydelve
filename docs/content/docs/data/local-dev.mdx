---
title: "Local Dev"
description: "Environment setup and make targets for running Polydelve locally."
---

## Data Model

```mermaid
flowchart LR
  subgraph Scripts["Seed Scripts"]
    S1[refresh_epss.py]
    S2[ingest_epss_history.py]
    S3[ingest_mal_advisories.py]
    S4[refresh_downloads.py]
    S5[enrich_package_sectors.py]
    S6[news_update.py]
  end

  subgraph Models["Pydantic Row Models"]
    M1["EpssHistoryRow\nname, ecosystem\nepss_score, recorded_at"]
    M2["MalAdvisoryRow\nosv_id, name, ecosystem\npublished_at, withdrawn"]
    M3["PackageRow\nname, ecosystem, epss_score\nweekly_downloads, risk_score\ncve_ids[], sectors[], has_mal"]
    M4["RecentNews\nid, title, source_url\npublished_date, analysis"]
  end

  subgraph Tables["DuckDB Tables (MotherDuck: md:polydelve)"]
    T1["epss_scores\nname, ecosystem, epss_score, recorded_at"]
    T2["mal_advisories\nosv_id PK, name, ecosystem\npublished_at, withdrawn"]
    T3["packages\n(name, ecosystem) PK\nweekly_downloads, epss_score\nrisk_score, sectors[], cve_ids[]\nhas_mal_advisory"]
    T4["news\nid PK (URL hash)\ntitle, source_url, published_date\nanalysis JSON, embeddings float[]"]
  end

  S1 --> M1 --> T1
  S2 --> M1
  S3 --> M2 --> T2
  S4 --> M3 --> T3
  S5 --> M3
  S6 --> M4 --> T4
```

## Prerequisites

- Python 3.14+, [uv](https://docs.astral.sh/uv/)
- Node.js 20+, npm

## Install

```bash
make be-install    # backend deps via uv
make fe-install    # frontend deps via npm
```

## Environment variables

Copy the secrets template and fill in keys:

```bash
cp backend/secrets/.env.example backend/secrets/.env
```

| Variable | Service | Required for |
|----------|---------|-------------|
| `EXA_API_KEY` | Exa | News fetch |
| `GEMINI_API_KEY` | Google Gemini | News tagging, sector LLM |
| `OPENAI_API_KEY` | OpenAI | Sector classification fallback |
| `MOTHERDUCK_TOKEN` | MotherDuck | Production DB (not needed locally) |
| `AUTH0_DOMAIN` | Auth0 | Auth (can use dev tenant) |
| `AUTH0_AUDIENCE` | Auth0 | Auth |

For local dev the app runs against a local DuckDB file — no MotherDuck token needed.

## Run

```bash
make dev     # backend (port 8000) + frontend (port 5173) concurrently
make be      # backend only
make fe      # frontend only
```

## Useful make targets

```
make news-update            fetch and store latest security news
make build-cve-history      pull CVE data from OSV for all packages
make enrich-packages        add download stats and descriptions
make enrich-sectors         heuristic sector classification
make classify-sectors-llm   LLM sector classification (slower)
make refresh-epss           update EPSS scores + resolve contracts
make be-test                run backend test suite
make lint                   lint frontend TypeScript
```

Run `make help` to see all targets.
